@strict_ifc()
fun main(env: Env, stdio: Stdio)
    let k = env.get("API_KEY").unwrap_or("none")
    let p: Int = 1
    let f = fun () =>
        if p == 1
            if k.starts_with("s")
                return
        stdio.println("after in lambda")
    f()

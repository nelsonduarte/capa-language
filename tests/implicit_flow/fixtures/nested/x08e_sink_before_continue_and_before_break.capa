@strict_ifc()
fun main(env: Env, stdio: Stdio)
    let k = env.get("API_KEY").unwrap_or("none")
    var i: Int = 0
    while i < 3
        i = i + 1
        stdio.println("first")
        if k.starts_with("s")
            continue

@strict_ifc()
fun main(env: Env, stdio: Stdio)
    let k = env.get("API_KEY").unwrap_or("none")
    let p: Int = 1
    var q: Int = 0
    for z1 in 0..1
        for z0 in 0..3
            if p == 1
                if k.starts_with("s")
                    continue
            stdio.println("reached")

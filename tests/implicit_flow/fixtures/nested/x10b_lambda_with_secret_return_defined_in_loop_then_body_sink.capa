@strict_ifc()
fun main(env: Env, stdio: Stdio)
    let k = env.get("API_KEY").unwrap_or("none")
    for i in 0..3
        let f = fun () -> Unit =>
            if k.starts_with("s")
                return
        f()
        stdio.println("body after lambda")

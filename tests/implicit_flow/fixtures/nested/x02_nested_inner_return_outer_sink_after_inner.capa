@strict_ifc()
fun main(env: Env, stdio: Stdio)
    let k = env.get("API_KEY").unwrap_or("none")
    for i in 0..3
        for j in 0..3
            if k.starts_with("s")
                return
        stdio.println("outer reached")

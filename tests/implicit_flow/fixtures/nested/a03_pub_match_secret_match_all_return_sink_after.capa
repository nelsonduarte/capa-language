@strict_ifc()
fun main(env: Env, stdio: Stdio)
    let k = env.get("API_KEY").unwrap_or("none")
    let p: Int = 1
    match p
        2 ->
            match k.starts_with("s")
                true -> return
                false -> return
        _ ->
            let _z = 0
    stdio.println("after")

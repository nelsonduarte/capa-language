@strict_ifc()
fun leak(token: @secret Bool, stdio: Stdio)
    var i: Int = 0
    while i < 3
        i = i + 1
        if token
            stdio.println("in branch is refused elsewhere")
fun main(stdio: Stdio)
    leak(true, stdio)

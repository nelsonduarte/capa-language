@strict_ifc()
fun main(env: Env, stdio: Stdio)
    let k = env.get("API_KEY").unwrap_or("none")
    let f = fun (x: Int) -> Int =>
        if k.starts_with("s")
            return 0
        return x
    let r: Int = f(2)
    stdio.println("public after typed lambda")

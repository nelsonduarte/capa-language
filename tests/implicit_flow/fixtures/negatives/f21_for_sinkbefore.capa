@strict_ifc()
fun leak(token: @secret Bool, stdio: Stdio, xs: List<Int>)
    for _i in xs
        stdio.println("every iteration")
        if token
            continue
fun main(stdio: Stdio)
    leak(true, stdio, [1, 2, 3])

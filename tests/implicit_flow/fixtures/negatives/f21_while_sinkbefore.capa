@strict_ifc()
fun leak(token: @secret Bool, stdio: Stdio)
    var i: Int = 0
    while i < 3
        i = i + 1
        stdio.println("every iteration")
        if token
            continue
fun main(stdio: Stdio)
    leak(true, stdio)

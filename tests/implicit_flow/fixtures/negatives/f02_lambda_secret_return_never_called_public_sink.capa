@strict_ifc()
fun main(env: Env, stdio: Stdio)
    let k = env.get("API_KEY").unwrap_or("none")
    let f = fun () =>
        if k.starts_with("s")
            return
        return
    stdio.println("public, lambda never called")

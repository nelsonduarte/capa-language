@strict_ifc()
fun main(env: Env, stdio: Stdio)
    let k = env.get("API_KEY").unwrap_or("none")
    let f = fun () =>
        if k.starts_with("s")
            return
        return
    f()
    stdio.println("public after lambda")

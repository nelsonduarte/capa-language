@strict_ifc()
fun main(env: Env, stdio: Stdio)
    let k = env.get("API_KEY").unwrap_or("none")
    for i in 0..3
        stdio.println("every iteration")
        if k.starts_with("s")
            continue

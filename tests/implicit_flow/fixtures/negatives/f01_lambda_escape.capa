@strict_ifc()
fun leak(token: @secret Bool, stdio: Stdio)
    let f = fun () -> Unit =>
        if token
            return
    f()
    stdio.println("public after lambda")
fun main(stdio: Stdio)
    leak(true, stdio)

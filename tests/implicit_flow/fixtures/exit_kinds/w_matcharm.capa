@strict_ifc()
fun leak(token: @secret Bool, stdio: Stdio)
    var i: Int = 0
    while i < 3
        i = i + 1
        match token
            true -> 
                continue
            false -> 
                stdio.println("f")
        stdio.println("reached")
fun main(stdio: Stdio)
    leak(true, stdio)

@strict_ifc()
fun leak(token: @secret Bool, stdio: Stdio)
    var i: Int = 0
    while i < 3
        i = i + 1
        match token
            true ->
                continue
                stdio.println("dead")
            false ->
                stdio.println("f")
fun main(stdio: Stdio)
    leak(true, stdio)

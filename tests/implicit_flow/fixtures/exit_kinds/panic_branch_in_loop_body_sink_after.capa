@strict_ifc()
fun main(env: Env, stdio: Stdio)
    let k = env.get("API_KEY").unwrap_or("none")
    var n: Int = 0
    while n < 3
        n = n + 1
        if k.starts_with("s")
            let _a = 1
            panic("no")
    stdio.println("n=${n}")

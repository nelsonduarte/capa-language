@strict_ifc()
fun main(env: Env, stdio: Stdio)
    let k = env.get("API_KEY").unwrap_or("none")
    var x: Bool = false
    for i in 0..3
        if x
            stdio.println("x")
        x = k.starts_with("s")

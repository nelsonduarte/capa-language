@strict_ifc()
fun main(env: Env, stdio: Stdio)
    let k = env.get("API_KEY").unwrap_or("none")
    var n: Int = 0
    var m: Int = 0
    while n < 9
        n = n + 1
        stdio.println("m=${m}")
        m = n
        if k.starts_with("s")
            break

@strict_ifc()
fun main(env: Env, stdio: Stdio)
    let k = env.get("API_KEY").unwrap_or("none")
    var n: Int = 0
    var c1: Bool = false
    for i in 0..5
        n = n + 1
        if c1
            break
        c1 = k.starts_with("s")
    stdio.println("n=${n}")

linear type Handle { id: Int }
fun open() -> Handle
    return Handle { id: 1 }
fun take(consume h: Handle) -> Bool
    return true
fun main(stdio: Stdio)
    let h = open()
    var n: Int = 0
    while take(h)
        n = n + 1
        if n > 2
            break
    stdio.println("n=${n}")

@strict_ifc()
fun main(env: Env, stdio: Stdio)
    let k = env.get("API_KEY").unwrap_or("none")
    let p: Int = 1
    var q: Int = 0
    let strict_mode: Bool = false
    for attempt in 0..3
        if strict_mode
            if k.length() < 8
                break
        stdio.println("attempt")

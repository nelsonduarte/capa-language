@strict_ifc()
fun leak(token: @secret Bool, stdio: Stdio, flag: Bool)
    if flag
        if token
            return
        stdio.println("reached")
fun main(stdio: Stdio)
    leak(true, stdio, true)

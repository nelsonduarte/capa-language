@strict_ifc()
fun leak(token: @secret Bool, stdio: Stdio, flag: Bool, g: Bool)
    if flag
        let _z = 0
    elif g
        if token
            return
        stdio.println("reached")
fun main(stdio: Stdio)
    leak(true, stdio, false, true)

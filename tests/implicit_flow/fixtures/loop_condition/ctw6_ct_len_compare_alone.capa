@constant_time()
fun tally(env: Env) -> Int
    let k = env.get("API_KEY").unwrap_or("none")
    var n: Int = 0
    let c1 = k.length() > 8
    if n == 0
        n = 2
    return n
fun main(env: Env, stdio: Stdio)
    let r = tally(env)
    stdio.println("r=${r}")

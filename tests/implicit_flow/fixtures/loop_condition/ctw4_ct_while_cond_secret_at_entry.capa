@constant_time()
fun tally(env: Env) -> Int
    let k = env.get("API_KEY").unwrap_or("none")
    var n: Int = 0
    while not (k.length() > 8) and n < 5
        n = n + 1
    return n
fun main(env: Env, stdio: Stdio)
    let r = tally(env)
    stdio.println("r=${r}")

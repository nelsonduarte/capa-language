fun probe(stdio: Stdio) -> Bool
    stdio.println("tick")
    return true
@strict_ifc()
fun main(env: Env, stdio: Stdio)
    let k = env.get("API_KEY").unwrap_or("none")
    var n: Int = 0
    while probe(stdio) and n < 5
        n = n + 1
        if k.starts_with("s")
            continue

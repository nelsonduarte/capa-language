@strict_ifc()
fun main(env: Env, stdio: Stdio)
    let k = env.get("API_KEY").unwrap_or("none")
    var lst: List<Int> = [0]
    var n: Int = 0
    for x in lst
        n = n + 1
        if k.starts_with("s") and n < 3
            lst.push(1)
    stdio.println("n=${n}")

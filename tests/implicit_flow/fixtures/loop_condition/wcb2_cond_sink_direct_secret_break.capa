fun tick(stdio: Stdio) -> Int
    stdio.println("tick")
    return 1
@strict_ifc()
fun main(env: Env, stdio: Stdio)
    let k = env.get("API_KEY").unwrap_or("none")
    var n: Int = 0
    while n < 5 and tick(stdio) == 1
        n = n + 1
        if k.starts_with("s")
            break

@strict_ifc()
fun main(env: Env, stdio: Stdio)
    let k = env.get("API_KEY").unwrap_or("none")
    var lst: List<Int> = []
    var n: Int = 0
    while lst.is_empty() and n < 5
        n = n + 1
        if k.starts_with("s")
            lst.push(1)
    stdio.println("n=${n}")

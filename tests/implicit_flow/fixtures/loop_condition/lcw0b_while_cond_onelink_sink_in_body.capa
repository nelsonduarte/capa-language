@strict_ifc()
fun main(env: Env, stdio: Stdio)
    let k = env.get("API_KEY").unwrap_or("none")
    var c1: Bool = false
    var n: Int = 0
    while not c1 and n < 5
        n = n + 1
        stdio.println("reached")
        c1 = k.starts_with("s")

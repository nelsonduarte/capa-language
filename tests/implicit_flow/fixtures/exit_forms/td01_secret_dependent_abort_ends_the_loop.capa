@strict_ifc()
fun main(env: Env, stdio: Stdio)
    let k = env.get("API_KEY").unwrap_or("none")
    var d: Int = 1
    if k.starts_with("s")
        d = 0
    var n: Int = 0
    while n < 5
        stdio.println("tick")
        n = n + 1
        let _q = 10 / d

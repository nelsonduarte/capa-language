@strict_ifc()
fun main(env: Env, stdio: Stdio)
    let k = env.get("API_KEY").unwrap_or("none")
    var lst: List<Int> = []
    for i in 0..6
        if lst.length() > 0
            return
        if k.starts_with("s")
            lst.push(1)
    stdio.println("after")

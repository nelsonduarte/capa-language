@strict_ifc()
fun main(env: Env, stdio: Stdio)
    let k = env.get("API_KEY").unwrap_or("none")
    if k.starts_with("s")
        var i: Int = 0
        while i < 3
            i = i + 1
            if i == 2
                continue
    stdio.println("after")

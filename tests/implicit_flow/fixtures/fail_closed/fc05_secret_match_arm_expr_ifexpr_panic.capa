@strict_ifc()
fun main(env: Env, stdio: Stdio)
    let k = env.get("API_KEY").unwrap_or("none")
    let n = 1
    match n
        _ if k.starts_with("s") -> if true then panic("a") else panic("b")
        _ ->
            let _z = 0
    stdio.println("after")

@strict_ifc()
fun main(env: Env, stdio: Stdio)
    let k = env.get("API_KEY").unwrap_or("none")
    if k.starts_with("s")
        for j in 0..3
            if j == 1
                return
    stdio.println("after")

@strict_ifc()
fun main(env: Env, stdio: Stdio)
    let k = env.get("API_KEY").unwrap_or("none")
    var n: Int = 0
    for i in 0..3
        n = n + 1
        let x = if k.starts_with("s") then 0 else match n
            1 ->
                break
            _ ->
                0
    stdio.println("n=${n}")

@strict_ifc()
fun main(env: Env, stdio: Stdio)
    let k = env.get("API_KEY").unwrap_or("none")
    let p = 1
    var n: Int = 0
    for i in 0..3
        n = n + 1
        let x = if p == 1 then 0 else match n
            1 ->
                break
            _ ->
                0
    stdio.println("n=${n}")

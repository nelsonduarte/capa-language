@strict_ifc()
fun main(env: Env, stdio: Stdio)
    let k = env.get("API_KEY").unwrap_or("none")
    var n: Int = 0
    for i in 0..5
        stdio.println("iter")
        if k.starts_with("s")
            break
        else
            continue
